# Production profile
server.port=8082

# Production API key (from environment variable, with fallback)
api.security.key=${API_KEY:prod-secret-key}

# Minimal logging for production
logging.level.com.learning.payslip=INFO
logging.level.root=WARN